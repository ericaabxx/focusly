import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flex: 1,
        paddingHorizontal: 20
    },
    header: {
        fontSize: 22,
        fontWeight: "bold",
        color: "#000",
        marginBottom: 15,
        marginTop: 10
    },
    listContent: {
        paddingBottom: 100,
    },
})